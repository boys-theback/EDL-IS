@extends('layouts.app')

@section('content')
<div class="page-wrap"><header class="topbar"><div><div class="eyebrow">Administration / Identity</div><h1>Users & destinations</h1><p class="muted">Manage accounts, roles, and where each user writes allow-list files.</p></div><button class="primary-button small" type="button" onclick="document.getElementById('new-user').showModal()">Add user <span>+</span></button></header>@if (session('status'))<div class="alert success">{{ session('status') }}</div>@endif @if ($errors->any())<div class="alert error">{{ $errors->first() }}</div>@endif
<section class="workspace-panel"><div class="panel-heading"><div><h2>People with access</h2><p class="muted">Super admins can manage accounts. The shared folder is managed above.</p></div></div><div class="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Created</th><th></th></tr></thead><tbody>@forelse ($users as $user)<tr><td><strong>{{ $user->name }}</strong></td><td>{{ $user->email }}</td><td><span class="tag tag-{{ $user->role === 'super_admin' ? 'both' : 'general' }}">{{ $user->role === 'super_admin' ? 'Super admin' : 'User' }}</span></td><td class="muted">{{ $user->created_at->diffForHumans() }}</td><td class="actions user-actions"><button type="button" class="edit-user-button" aria-label="Edit {{ $user->name }}" onclick="document.getElementById('edit-{{ $user->id }}').showModal()"><span>✎</span> Edit</button>@if (! $user->is(auth()->user()))<form method="POST" action="{{ route('admin.users.destroy', $user) }}" class="inline-form">@csrf @method('DELETE')<button class="delete-user-button" type="submit" aria-label="Delete {{ $user->name }}" onclick="return confirm('Delete this user account?')">×</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="empty-state">No users found.</td></tr>@endforelse</tbody></table></div>@if ($users->hasPages())<div class="pagination">{{ $users->links() }}</div>@endif</section>
@foreach ($users as $user)<dialog id="edit-{{ $user->id }}" class="user-dialog"><form method="POST" action="{{ route('admin.users.update', $user) }}" class="modal-card">@csrf @method('PUT')<div class="modal-heading"><div><div class="eyebrow">Account settings</div><h2>Edit user</h2><p class="muted">Update access details for {{ $user->name }}.</p></div><button type="button" class="close-button" aria-label="Close edit user dialog" onclick="this.closest('dialog').close()">×</button></div><div class="modal-section-label">Identity</div><div class="entry-fields"><label>Name<input name="name" value="{{ $user->name }}" required maxlength="120"></label><label>Email<input type="email" name="email" value="{{ $user->email }}" required autocomplete="email"></label></div><div class="modal-section-label">Access</div><div class="entry-fields"><label>Role<select name="role"><option value="user" @selected($user->role === 'user')>User</option><option value="super_admin" @selected($user->role === 'super_admin')>Super admin</option></select></label><label>New password<input type="password" name="password" minlength="8" autocomplete="new-password" placeholder="Leave blank to keep current"></label></div><div class="modal-actions"><button type="button" class="ghost-button" onclick="this.closest('dialog').close()">Cancel</button><button class="primary-button" type="submit"><span>✓</span> Save changes</button></div></form></dialog>@endforeach
<dialog id="new-user"><form method="POST" action="{{ route('admin.users.store') }}" class="modal-card">@csrf<div class="modal-heading"><div><div class="eyebrow">New account</div><h2>Add user</h2></div><button type="button" class="close-button" onclick="this.closest('dialog').close()">×</button></div><div class="entry-fields"><label>Name<input name="name" required></label><label>Email<input type="email" name="email" required></label><label>Role<select name="role"><option value="user">User</option><option value="super_admin">Super admin</option></select></label><label>Password<input type="password" name="password" minlength="8" required></label></div><div class="modal-actions"><button type="button" class="ghost-button" onclick="this.closest('dialog').close()">Cancel</button><button class="primary-button" type="submit">Create user <span>→</span></button></div></form></dialog></div>
@endsection
